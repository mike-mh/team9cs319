'use strict';

/*
 * Unit test for dcgui module
 */
describe('DCGUI module', function() {

  // inject the HTML fixture for the tests
  beforeEach(module('dcgui'));

  describe('initialization', function() {
    it('should have all of its sub-modules services injected', function() {
      console.log('TO-DO');
    });

    // TO-DO
    it('should call services to retrieve data before app is initialized', function() {
      console.log('TO-DO');
    });
  });
});
